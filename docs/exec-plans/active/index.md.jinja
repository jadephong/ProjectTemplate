# Active Exec Plans — {{ project_name }}

进行中的工作放这里。

## Plan 文件约定

- 一份 plan 一个文件，命名 `<JiraID>-<slug>.md`，对应 Jira。
- 内容至少含：Jira ID、范围、关键步骤、验收（测试绿 + Sonar gate + review pass）、风险/边界。
- 完成后移到 `../completed/`。

## Sprint 约定

- 进行中 sprint → `active/sprint-N/`（工作文档平铺）。
- 完成后整个 sprint 移到 `../completed/sprint-N/`，内部分 `plan/` + `sprint-review/`。

## 当前 active

- 暂无。新任务从这里起一份 plan。
