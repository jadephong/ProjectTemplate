# Skill 路由 — {{ project_name }}

按任务场景选 skill 的速查表。从 `AGENTS.md` 拆出，保持 AGENTS.md 是"地图"。

> 使用原则见 `AGENTS.md` §「Skill 使用原则」。决定改哪个**代码模块**见 `AGENTS.md` §「代码模块路由」（层次不同，勿混淆）。

## Skill 管理

| 场景 | 优先使用 Skill |
|---|---|
| 找 skill、查有没有现成能力 | `find-skills` |
| 安装 skill | `skill-installer` |
| 创建或修改 skill | `write-a-skill` / `skill-creator` |

> 安装 skill 后需提醒：重启 CLI 才能稳定生效。

## 需求、计划、开发流程

| 场景 | 优先使用 Skill |
|---|---|
| 需求梳理、创意、功能设计 | `brainstorming` |
| 写实现计划 | `writing-plans` |
| 把计划或决策落到 Markdown 文件 | `planning-with-files` |
| 实现功能或修 bug 前 | `tdd` |
| 执行已有计划 | `executing-plans` |
| 完成前验收 | `verification-before-completion` |

## 调试与代码库理解

> **先用 codegraph（MCP 代码图谱，非 skill）——理解代码的默认入口，优先于 grep+read：**
>
> | 想知道什么 | 用哪个 codegraph 工具 |
> |---|---|
> | "X 怎么工作 / 在哪 / 架构 / 调用流" | `codegraph_explore`（PRIMARY，一把梭返回相关符号源码） |
> | 仅查某符号位置 | `codegraph_search` |
> | 谁调用它 / 它调用谁 / 改它会影响啥 | `codegraph_callers` / `codegraph_callees` / `codegraph_impact` |
> | 某符号完整源码 | `codegraph_node` |
>
> 写/改代码**前**查，不在写的过程中查；直接回答，别另开子 agent 重复探索。下面的 skill 用于 codegraph 覆盖不到的场景。

| 场景 | 优先使用 Skill |
|---|---|
| 陌生代码库，先摸清结构 | `recon` |
| bug、测试失败、异常行为 | `diagnose` |
| 从更高层理解代码 | `zoom-out` |
| 架构改进、降低耦合 | `improve-codebase-architecture` |
| 代码极简化、避免过度工程 | `andrej-karpathy-perspective` |

## 质询与方案推演

| 场景 | 优先使用 Skill |
|---|---|
| 用户说 "grill me"，想被追问方案 | `grill-me` |
| 结合项目文档和领域模型质询方案 | `grill-with-docs` |

规则：一次只问一个问题；每个问题给出推荐答案；能看代码解决的，先看代码再问。

## 多代理与并行

| 场景 | 优先使用 Skill |
|---|---|
| 2 个以上相互独立的任务 | `dispatching-parallel-agents` |
| 按计划拆分并行开发 | `subagent-driven-development` |

使用多代理前必须先说明：拆分边界、哪些文件由哪个代理负责、如何避免多代理修改同一批文件。

## 前端、React、网页质量

| 场景 | 优先使用 Skill |
|---|---|
| 前端页面、组件、UI、React / Next.js | `frontend-design` / `ui-ux-pro-max` |
| React / Next.js 性能和写法 | `react-best-practices` |
| UI / UX 评审 | `frontend-dev-guidelines` / `web-design-guidelines` |
| 网页综合质量审计 | `web-quality-audit` |
| 性能 | `performance` |
| 可访问性 | `accessibility` |
| SEO | `seo` |
| Core Web Vitals | `core-web-vitals` |
| Web 最佳实践 | `best-practices` |

## GitHub、Git、CI、Review

| 场景 | 优先使用 Skill |
|---|---|
| GitHub 仓库、PR、Issue | `github` |
| PR review comments | `gh-address-comments` |
| GitHub Actions / CI 失败 | `gh-fix-ci` |
| 收到代码评审意见 | `receiving-code-review` |
| 完成大改动后请求复核 | `requesting-code-review` |
| 安全回滚或误操作恢复 | `git-snapshot-rollback` |

## 文档、表格、PPT、发布

| 场景 | 优先使用 Skill |
|---|---|
| Word / 文档 | `documents` |
| 表格 / Excel / Google Sheets | `spreadsheets` |
| PPT / 幻灯片 | `presentations` |
| HTML 幻灯片、网页式汇报页 | `html-slides` |
| Markdown 清理 | `markdown-formatter` |

## 写作与内容

| 场景 | 优先使用 Skill |
|---|---|
| 中文长文、公众号文章 | `khazix-writer` |
| 去 AI 味、自然表达 | `humanizer-zh` |
| OpenAI 官方文档 / API | `openai-docs` |

## 图片与视觉素材

| 场景 | 优先使用 Skill |
|---|---|
| 图片生成 / 编辑 | `imagegen` |

## Azure、服务器、外部集成

| 场景 | 优先使用 Skill |
|---|---|
| Azure AI | `azure-ai` |
| Azure 部署准备 | `azure-prepare` |
| Azure 执行部署 | `azure-deploy` |
| Azure 诊断 | `azure-diagnostics` |
| Azure Storage | `azure-storage` |
| 宝塔 / aaPanel / 服务器巡检 | `btpanel` |
| 外部应用连接与真实操作 | `connect` |

> 服务器和云资源默认只读检查；修复、重启、改配置、部署前必须确认。

## 安全与合同

| 场景 | 优先使用 Skill |
|---|---|
| 安全检查、权限边界、外部集成风险 | `security-best-practices` |
| 合同、NDA、SaaS 条款、法律风险 | `contract-review` |

> 合同类任务：必须提醒这不是正式法律意见；默认只读分析；不直接给最终法律结论；不替用户做决定。
